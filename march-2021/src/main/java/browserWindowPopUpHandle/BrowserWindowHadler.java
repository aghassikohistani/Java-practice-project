package browserWindowPopUpHandle;

public class BrowserWindowHadler {

	public static void main(String[] args) {
		
		//cross origin policy, you are working in one domain and clicking on another link 
		//and another link gets open, this is called cross origin policy 
		//to switch from one window to another window, I will be using Window Handler API 
		//should capture the session id's of both windows, the parent window and the child 
		//window, and the driver.getWindowHandles will give us the set of String the parent 
		//to fetch the data we have to use the iterator method, Handles.iterator(); 
		

	}

}
